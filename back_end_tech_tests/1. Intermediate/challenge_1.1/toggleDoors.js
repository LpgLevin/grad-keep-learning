const toggleDoors = () => {}

// create empty array to hold doors

// create loop ot iterate from 1 to 'n' (the number passed in), pushing a one into the doors array for each iteration

// iterate through the dorrs array closing every second door (changing it to a zero)

//then every 3rd door

//then every 4th

// until you get to every nth door (so you only close the final door) by changing it to zero

// return the indexes of all the remaining open doors (1s)

module.exports = toggleDoors;